import { Express } from 'express';

/**
 * User routes - Placeholder for now
 */
export function userRoutes(app: Express): void {
  // As rotas de usuários são extensas
  // Por agora, deixaremos no index.ts e refatoraremos depois
}
